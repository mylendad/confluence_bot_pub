# План реализации расширений Backend API (Todo List)

## 1. Логирование и вывод ошибок в UI
- [x] **app/logging_config.py:** Добавить `MemoryLogHandler` (на базе `collections.deque`) для хранения последних N строк логов в оперативной памяти.
- [x] **app/main.py:** Реализовать эндпоинт `GET /api/logs`, возвращающий содержимое `MemoryLogHandler` (массив строк с логами).

## 2. Статус синхронизации
- [x] **app/main.py (или отдельный роутер):** Реализовать эндпоинт `GET /api/sync/status`.
- [x] Эндпоинт должен обращаться к `S2TStateRepository` и возвращать агрегированную информацию: время последней успешной синхронизации, общее количество витрин, количество витрин с ошибками парсинга/загрузки.

## 3. Шаблоны быстрых вопросов
- [x] **app/main.py:** Реализовать эндпоинт `GET /api/questions/templates`.
- [x] Эндпоинт должен возвращать статический список типовых вопросов (например, владелец, состав полей, логика расчета) в формате: `[{"id": "...", "label": "...", "template": "..."}]`.

## 4. Сохранение истории чата
- [x] **app/storage/sqlite.py (Схема БД):** Добавить скрипт создания новой таблицы `chat_history` (поля: `id`, `session_id`, `user_message`, `bot_response`, `sources_json`, `created_at`).
- [x] **app/storage/history_repository.py (или новый репозиторий):** Создать репозиторий для записи и чтения истории диалогов.
- [x] **app/bot/service.py:** Обновить логику `ask()`: после получения ответа от LLM/базы данных сохранять пару "вопрос-ответ" в БД. (Реализовано в `app/main.py`).
- [x] **app/bot/http_adapter.py:** Добавить опциональное поле `session_id` in `AskRequest`.
- [x] **app/main.py:** Реализовать эндпоинт `GET /api/chat/history/{session_id}` для загрузки истории предыдущих диалогов.

## 5. Мониторинг внешних подключений
- [x] **app/main.py:** Реализовать эндпоинт `GET /api/health/external`.
- [x] Эндпоинт должен проверять доступность Confluence API и GigaChat API (выполнять легкие проверочные запросы) и возвращать статусы ("ok", "error") и задержку.

## 6. Информация о последнем парсинге и обновлении RAG
- [x] **app/main.py:** Реализовать эндпоинт `GET /api/sync/last-events`.
- [x] Эндпоинт должен возвращать даты последнего парсинга файлов (из `s2t_state`) и последнего обновления метаданных витрин (из `datamarts`).
